[project]
name = "guide_creator"
version = "0.1.0"
description = "guide_creator using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[google-genai,tools]==1.14.4",
]

[project.scripts]
kickoff = "guide_creator.main:kickoff"
run_crew = "guide_creator.main:kickoff"
plot = "guide_creator.main:plot"
run_with_trigger = "guide_creator.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "flow"
